<%= button_to 'New Photo', new_photo_path, method: :get, :form => { :class => "navigation-button" } %>
<%= button_to 'Log Out', '/session/destroy', method: :get, :form => { :class => "navigation-button" } %>
